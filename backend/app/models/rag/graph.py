"""SQLAlchemy records for graph versioning (UNUSED - kept for reference)."""

# NOTE: All graph versioning models have been removed as part of rag_db cleanup.
# Tables dropped: entity_resolution_runs, canonical_entity_versions,
# entity_resolution_assignments, entity_resolution_lineage,
# relationship_projection_versions, canonical_relationship_versions,
# graph_mappings, graph_snapshots, graph_snapshot_revisions
#
# These were part of a planned v2 knowledge graph pipeline that was never activated.
# See rag_migrations/versions/0021_drop_unused_tables.py for the DROP statements.
